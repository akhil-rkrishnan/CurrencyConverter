package app.android.currency_converter.data.constants

const val TIMER_INTERVAL = (1000 * 60 * 30).toLong() // 30 minutes in millis
const val TIMER_DELAY = TIMER_INTERVAL // giving the initial delay as timer interval
const val REFRESH_TIME = 30 // refresh time in minutes